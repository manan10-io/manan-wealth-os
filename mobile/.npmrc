# Expo SDK 57 pins react@19.2.3, but transitive react-dom@19.2.x (via
# expo-router's radix deps) peer-requires the newest 19.2 patch. npm's strict
# resolver rejects that; the tree is fine at runtime (react-dom is web-only
# here). Keep installs deterministic and unblocked:
legacy-peer-deps=true
